<%= form_with(model: import_file, local: true) do |form| %>
  <% if import_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(import_file.errors.count, "error") %> prohibited this import_file from being saved:</h2>

      <ul>
      <% import_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :file_name %>
    <%= form.text_field :file_name %>
  </div>

  <div class="field">
    <%= form.label :uploaded_at %>
    <%= form.datetime_select :uploaded_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
